# AI Agent Rules

1. **Third-Party Dependencies**: Explicit human approval is REQUIRED before modifying `go.mod` to add any new third-party library dependencies (e.g., running `go get <package>`). You must ask the user and wait for their confirmation before executing such actions.
